{{#each this}}
<li>
  <a href="{{url}}" target="_blank" rel="noopener noreferrer">
    <article>
      <img src="{{urlToImage}}" alt="">
      <h2>{{title}}</h2>
      <p>Published by: {{author}}</p> 
      <p>{{description}}</p>
    </article>
  </a>
</li>
{{/each}}